# Private working files — live in ../Bolo Boys - Private/, never deployed
Bolo Boys CRM.xlsx
*.xlsx
data/distribution-list.json
docs/promotion-research.md
CLAUDE.md

# Working design files — logo/brand work IS version-controlled (un-ignored 2026-06-28);
# only the heavy binary poster references + PSDs stay out
design/references/
*Redesign*.png
*Redesign*.psd

# Internal scratch directories
.content/
.engineering/
.research/
.user-research/
.ux-reviews/
.claude/

# Generated artifacts — rebuilt from data/ via tools/
bandsintown-upload.csv

# macOS
.DS_Store

# Editor / tool backup files
*.bak

# Local-only state markers (machine-specific, not shared)
tools/.last-bit-upload

# Wrangler local state (auth cache, build artifacts) — never commit
.wrangler/
